"""Import compatibility package for source checkouts.

Installed use resolves ``v1_orchestrator`` from the ``src`` layout; this shim
keeps direct ``import src`` and ``python orchestrator.py`` workflows friendly.
"""
